---
# Managed by modulesync - DO NOT EDIT
# https://voxpupuli.org/docs/updating-files-managed-with-modulesync/

name: Create Git tag

on:
  workflow_dispatch:

permissions: {}

jobs:
  create_tag:
    uses: 'voxpupuli/gha-puppet/.github/workflows/create_tag.yml@b3f32179c62bfa8b50e2fe7a9911c07f7dc86eca' # 2.0.0
    with:
      allowed_owner: '<%= @configs[:namespace] %>'
      git_name: '<%= @configs['git_name'] %>'
      git_email: '<%= @configs['git_email'] %>'
    secrets:
      # Configure secrets here:
      #  https://docs.github.com/en/actions/security-guides/encrypted-secrets
      ssh_private_key: ${{ secrets.PCCI_SSH_PRIVATE_KEY }}
